### Phase 3：完善待完善功能

**优先级排序：**
1. ✅ `generate_dummy.py` 增加 TDDB 数据生成（调试需要）
2. LaTeX 公式渲染（label_11/label_12）
3. ConfigManager 集成（配置持久化）
4. 置信区间写入结果表格
5. E2E 集成测试（pytest-qt）

---

### Task 1: generate_dummy.py 增加 TDDB 数据

在现有文件末尾追加 `generate_tddb_data()` 函数和 CLI 参数 `--tddb`。

生成内容：
- 3 个电压（5.0, 5.5, 6.0 V）
- 2 个温度（25, 125 ℃）
- 2 个 group（"湿氧", "干氧"）
- 每个条件 20 个样本
- 写入 `data/tddb_test_data.xlsx`

### Task 2: LaTeX 公式渲染

- `lblLifeFormula` 和 `lblFailFormula` 使用 matplotlib LaTeX → PNG → QPixmap
- 随模型切换（E/1E/V）自动更新公式
- 生命周期缓存图片避免重复渲染

### Task 3: ConfigManager 集成

- 创建 `tddb_schema.py` 定义配置项
- `TDDBPage.__init__` 中初始化 ConfigManager
- 保存/加载：文件路径、sheet、工作电压、温度等参数

### Task 4: 置信区间表格

- 在寿命结果表格和失效率结果表格中增加置信区间列
- 使用 `confidence.lifetime_ci()` 计算结果

### Task 5: E2E 集成测试

- `tests/test_tddb_e2e.py` — 用 pytest-qt 启动 QApp
- 创建 TDDBPage → 加载测试 Excel → 点击拟合 → 验证结果
