## TDDB 分析功能——关闭 Designer 前最后检查

### ✅ 已确认添加到 .ui
- [✅] TBD/QBD 单选按钮（radioButton_2 / radioButton_3 + label_3 "计算数据："）
- [✅] QWebEngineView（groupBox_2 内，Weibull 图显示）
- [✅] "β诊断"按钮（pushButton_8）
- [✅] 工作温度输入（label_8 + lineEdit_2）
- [✅] "模拟"按钮（pushButton_9，lineEdit_4 右边）
- [✅] label_11 / label_12 设置 scaledContents=true
- [✅] label_2 右对齐

### ❌ 关闭前还需在 Designer 做的

**1. tab_8（寿命计算→原始数据查看）还是空的**
需要放一个 QTableWidget 进去，用于显示和编辑各组的模型参数。新建一个 QTableWidget 即可，列数等我在代码里设置。名字可以叫 `tableWidget_2`（我后面会重命名）。

有的啊

**2. groupBox_6（模型绘图）还是空的**
寿命计算的 E/1E/V 模型拟合曲线图要放在这里。需要放一个 QWebEngineView 进去，名字可以叫 `webEngineView_2`。注意 6.9 版本 QWebEngineView 的头文件路径在 QtWebEngineCore 而非 QtWebEngineWidgets，编译前我在 _ui.py 里手动修正。
有的啊
**3. TDDB_Custom_draw.ui 没找到**
你说"我写了个 TDDB_Custom_draw.ui"，但搜索项目目录没找到。是不是还没保存？如果已经保存了，确认下路径。
有的啊
### ❓ tab_8 的列结构补充确认

你说"不止E模型，1/E模型 V模型的参数也要放"，那 tab_8 的列应该是：

| 列 | 内容 | 可编辑 |
|---|---|---|
| Group | group 名称 | 否 |
| Vgs / V | 应力电压 | 否 |
| Temp / ℃ | 温度 | 否 |
| Area | 栅氧面积 | 否 |
| β | Weibull 斜率 | ✅ |
| η_TBD | TBD 特征寿命 | ✅ |
| η_QBD | QBD 特征寿命 | ✅ |
| R²_raw | 原始数据拟合 R² | 否 |
| R²_log | 对数坐标 R² | 否 |
| γ | E 模型系数（cm/MV） | ✅ |
| G | 1/E 模型系数（MV/cm） | ✅ |
| βv | V 模型系数（1/V） | ✅ |
| Ea | 激活能（eV） | ✅ |

所有模型参数都放上去共 13 列，用户编辑当前选中的模型对应的参数即可，不用的列留空。这个结构 OK 吗？
没问题